package build

class JarBuilder {
    /*    companion object {
            val GIT: Git = Git.open(File(""))
        }

        constructor(branch: String) {
            val call = GIT.checkout().setName(branch).call()
        }*/
}